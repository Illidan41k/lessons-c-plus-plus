#include <iostream>
#include "StlAlgorithms.h"
#include "Caching.h"
#include "WeakPointer.h"
#include "Logger.h"
#include "SomeFeatures.h"

int main()
{
    //DOaLL();

    //Caching object{};

    //auto sum = object.GetSum();
    //sum.Print();
    //sum = object.GetSum();
    //sum.Print();
    //object.AddNewElement(HugeMathValue(20));
    //sum = object.GetSum();
    //sum = object.GetSum();
    //sum = object.GetSum();

    //auto scorp = std::make_shared<Character>("Scorpion"); 
    //auto cassi = std::make_shared<Character>("Cassi Cage"); 
    //StartFight(scorp, cassi);
    //scorp->PrintInfo();

    //cout << sizeof(Class1) << endl;
    //cout << sizeof(Class2) << endl;

    //ThernaryEqualization();

    //Iota();

    //Logger logger;
    //logger.Info("hi all!");
}

